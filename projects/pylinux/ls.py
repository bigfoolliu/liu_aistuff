#!/usr/bin/env python3
# -*- coding:utf-8 -*-


import argparse
import logging
import sys

# arg_parser = argparse.ArgumentParser(prog="ls", description="需要显示的路径")
# arg_parser.add_argument("-a", dest="all", help="显示所有的文件夹")

# args = arg_parser.parse_args()

# if args.all:
#     logging.debug("已经显示所有文件夹")

# logging.debug("done")
